# Drop sound effect mp3 files here. Filenames referenced in code:
#   plate-smash.mp3   — used by /pages/smash
#   destroy-shredder.mp3 destroy-fire.mp3 destroy-hole.mp3 destroy-truck.mp3
#                     — used by /pages/destroy (one per type, optional)
#
# Keep each file <50KB. Suggested source: freesound.org / 爱给网 (royalty-free).
# Sounds are off by default; users opt in via the toggle in /pages/smash.
